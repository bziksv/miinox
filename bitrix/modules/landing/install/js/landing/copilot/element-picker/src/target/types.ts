export type BlockTargets = {
	nodes: Element[];
	icons: Element[];
	groups: Element[];
	blockTarget: Element;
};

export type PublishedTarget = {
	element: Element;
	selector: string;
	fingerprint: string;
};
